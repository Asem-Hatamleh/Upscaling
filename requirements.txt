# --- Core ML ---
# Install PyTorch matching your CUDA from https://pytorch.org first.
# Example (dev, RTX 5050 / sm_120 / CUDA 13.x):
#   pip install --pre torch torchvision --index-url https://download.pytorch.org/whl/nightly/cu128
# Example (prod, A100 / CUDA 12.x):
#   pip install torch==2.4.* torchvision==0.19.* --index-url https://download.pytorch.org/whl/cu121
torch
torchvision

# --- Pipeline deps ---
numpy<3
einops>=0.7
safetensors>=0.4
huggingface_hub>=0.24,<2
imageio>=2.34
imageio-ffmpeg>=0.4.9
av>=12.0           # PyAV: imageio backend
Pillow>=10
opencv-python>=4.9
tqdm>=4.66
PyYAML>=6.0

# --- Face restoration deps (used by gfpgan + codeformer paths) ---
# `transformers` is pulled in transitively by facexlib's tokenizer-free paths
# on some platforms; pin to the version we know works with basicsr 1.4.2.
transformers==4.46.2

# --- Real-ESRGAN / GFPGAN / CodeFormer backends ---
# `realesrgan_gfpgan` stacks Compact (background) + GFPGAN-1.4 (faces).
# `codeformer_compact` stacks Compact + CodeFormer-v1 (faces). The
# CodeFormer arch is vendored into ``src/models/_codeformer/`` and reuses
# pip basicsr's `ARCH_REGISTRY` / `get_root_logger`. `lpips` is a transitive
# dep of CodeFormer's perceptual loss module (loaded lazily).
realesrgan>=0.3.0
basicsr>=1.4.2
gfpgan>=1.3.8
facexlib>=0.3.0
lpips>=0.1.4

# --- Dev ---
pytest>=8
